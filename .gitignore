# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds
# pubspec.lock is committed (this is an app, not a package) for reproducible builds.

# Golden-test failure artifacts (diff/isolated/masked PNGs from a failed run).
test/golden/failures/

# IDE / tooling
.idea/
.vscode/
*.iml
*.ipr
*.iws
.atom/
.history/
*.swp

# Android
android/.gradle/
android/captures/
android/local.properties
android/key.properties
android/**/GeneratedPluginRegistrant.java
*.keystore
*.jks

# iOS / macOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
**/DerivedData/
*.mode1v3
*.mode2v3
*.perspectivev3

# Secrets / local config (USDA API key, etc.)
.env
.env.*
secrets.dart
**/secrets.dart

# OS
.DS_Store
Thumbs.db

# Backups / scratch
*.zip
*.bak
scratch/
.emu_*.png
.scratch*

# Claude Code local settings
.claude/settings.local.json
claude.session

# Play Store service-account key — NEVER commit
fastlane/play-store-key.json
fastlane/*.json
# App Store Connect API key + iOS signing material — NEVER commit
*.p8
*.p12
*.cer
*.mobileprovision
*.provisionprofile
ios/fastlane/report.xml
# fastlane run artifacts
fastlane/report.xml
fastlane/Preview.html

# Screenshot generation (transient driver output + staging)
/screenshots/
/delivery/

# Local-only dependency overrides (fuchsbau co-dev) — never commit.
pubspec_overrides.yaml
